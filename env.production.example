# Production Environment Variables Template

# Database Connection
# Use connection pooling parameters for production
DATABASE_URL="postgresql://username:password@host:5432/database?connection_limit=5&pool_timeout=10"

# NextAuth Configuration
# Must match your production domain
NEXTAUTH_URL="https://shah.belovezem.com"

# Generate a secure random secret (minimum 32 characters)
# Use: openssl rand -base64 32
# Or: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
NEXTAUTH_SECRET="your-production-secret-here-minimum-32-characters"
AUTH_SECRET="your-production-secret-here-minimum-32-characters"

# Trust the proxy/hosting environment
AUTH_TRUST_HOST="true"

# Lichess OAuth Configuration
# Your Lichess OAuth app Client ID
# Redirect URI must be: https://shah.belovezem.com/api/auth/callback/lichess
LICHESS_CLIENT_ID="chess-study-app"

# Note: LICHESS_CLIENT_SECRET is not needed for PKCE flow
